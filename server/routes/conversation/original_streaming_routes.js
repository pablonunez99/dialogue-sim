// Ruta principal de conversacion: resuelve viaje, eventos, IA, y actualiza el estado del mundo import {   loadHistory, saveHistory, loadNpcs, saveNpcs, loadLocations, saveLocations, loadEvents,   findPath, CONNECTIONS } from '../data/repository.js'; import { addMinutesToTime, getTimeOfDay } from '../world/time.js'; import { getEligibleEvent, triggerBackgroundEvents, generateSingleQuestOnTheFly } from '../world/eventEngine.js'; import { runMorningWorldUpdate } from '../world/dayTransition.js'; import { triggerTimeOfDayTransitionIfNeeded } from '../triggers/dayPhaseTriggers.js'; import { geminiClient, geminiModel, openaiClient, openaiModel, client, modelName, useGemini, defaultProvider } from '../config/aiProviders.js'; import { ConversationManager } from '../conversationManager.js'; import { generateNpcPortraits, generateLocationBackground } from '../images/portraitGenerator.js'; import { syncNpcToVectorDb, syncLocationToVectorDb } from '../vectorSync.js'; import { db } from '../db.js'; import { getPathDistance } from '../../src/data/world.js';  export function registerConversationRoutes(app) { app.post('/api/conversation', async (req, res) => {   const history = await loadHistory();   let { locationId, participantIds, playerText, state = {}, provider } = req.body ?? {};   console.log(`\n[Server] POST /api/conversation - locationId: "${locationId}", participantIds: [${(participantIds || []).join(', ')}], playerText: "${playerText}", history length: ${history.length}, provider: "${provider}"`);      const currentLocations = await loadLocations();   const currentNpcs = await loadNpcs();      const inputState = {     locationId: state.locationId || locationId,     relationships: state.relationships || {},     trust: state.trust || {},     day: typeof state.day === 'number' ? state.day : 1,     time: typeof state.time === 'string' ? state.time : '08:00',     flags: Array.isArray(state.flags) ? state.flags : [],     completedEvents: Array.isArray(state.completedEvents) ? state.completedEvents : [],     inventory: Array.isArray(state.inventory) ? state.inventory : [],     gold: typeof state.gold === 'number' ? state.gold : 0,     quests: Array.isArray(state.quests) ? state.quests : [],     npcActivityLog: Array.isArray(state.npcActivityLog) ? state.npcActivityLog : []   };    // Append player turn to server-side history   history.push({     speakerId: 'player',     speaker: 'Viajero',     line: playerText || '',     type: 'player',     locationId: inputState.locationId,     day: inputState.day,     time: inputState.time   });    // Intercept if travelQueue is active (meaning a step-by-step automatic trip is ongoing)   let travelQueue = Array.isArray(state.travelQueue) ? [...state.travelQueue] : [];   const isManualTravel = locationId !== state.locationId && (travelQueue.length === 0 || travelQueue[0] !== locationId);   if (isManualTravel) {     // Resolve the path from oldLocId to locationId     const path = findPath(state.locationId || 'plaza', locationId);     if (path && path.length > 2) {       const nextStep = path[1];       travelQueue = path.slice(2);       console.log(`[JourneyEngine] Player initiated multi-node journey from "${state.locationId}" to "${locationId}". Path: [${path.join(' -> ')}]. Next step: "${nextStep}", Remaining queue: [${travelQueue.join(', ')}].`);       locationId = nextStep; // Override requested locationId to the next step     } else {       travelQueue = [];     }   }    const oldLocId = state.locationId || locationId;   let travelMinutes = 0;   let hasTraveled = false;    if (travelQueue.length > 0) {     const nextStep = travelQueue.shift();     const conn = (CONNECTIONS[oldLocId] || []).find(c => c.to === nextStep);     travelMinutes = conn ? conn.distance : 5;          const { day: advancedDay, time: advancedTime } = addMinutesToTime(inputState.day, inputState.time, travelMinutes);     inputState.day = advancedDay;     inputState.time = advancedTime;     inputState.locationId = nextStep;     locationId = nextStep; // Override locationId parameter for this turn!     hasTraveled = true;          console.log(`[JourneyEngine] Processing step in active journey: "${oldLocId}" -> "${nextStep}". Remaining queue: [${travelQueue.join(', ')}].`);   } else if (oldLocId !== locationId) {     // Normal single-step travel     const conn = (CONNECTIONS[oldLocId] || []).find(c => c.to === locationId);     travelMinutes = conn ? conn.distance : 5;          const { day: advancedDay, time: advancedTime } = addMinutesToTime(inputState.day, inputState.time, travelMinutes);     inputState.day = advancedDay;     inputState.time = advancedTime;     inputState.locationId = locationId;     hasTraveled = true;     console.log(`[TravelEngine] Player manually traveled from "${oldLocId}" to "${locationId}" (${travelMinutes} min). Time advanced to Day ${inputState.day} - ${inputState.time}`);   }    inputState.travelQueue = travelQueue;    const location = currentLocations.find((item) => item.id === locationId);   if (!location || !String(playerText ?? '').trim()) {     return res.status(400).json({ error: 'Faltan datos de conversacion o ubicacion invalida.' });   }    const events = await loadEvents();   const bgStartResult = await triggerBackgroundEvents(inputState, events, currentLocations);   const activeEvent = getEligibleEvent(locationId, inputState, events);   let finalParticipantIds = participantIds;   if (hasTraveled) {     // Clear participants list so that the server resolves the NPCs who are actually at the new location     finalParticipantIds = [];   }    if (activeEvent) {     console.log(`[EventEngine] Active Event Triggered: "${activeEvent.name}" (${activeEvent.id})`);     if (activeEvent.setsFlags && activeEvent.setsFlags.length > 0) {       activeEvent.setsFlags.forEach(f => {         if (!inputState.flags.includes(f)) {           inputState.flags.push(f);         }       });     }     if (!activeEvent.repeatable) {       if (!inputState.completedEvents.includes(activeEvent.id)) {         inputState.completedEvents.push(activeEvent.id);       }     }     if (activeEvent.involvedNpcs && activeEvent.involvedNpcs.length > 0) {       finalParticipantIds = activeEvent.involvedNpcs;     }   }    let unexpectedEventNote = '';   const rollTwist = Math.random() < 0.15;   let twistTriggered = false;    if (rollTwist) {     twistTriggered = true;     const twists = [       `INTRUSI├ôN INESPERADA: Un NPC aleatorio de la aldea (que no estaba participando) irrumpe de golpe en la escena o interrumpe la conversaci├│n con una noticia urgente, una acusaci├│n dram├ítica o una exigencia extra├▒a. El DM debe forzar a este NPC a ingresar en el campo 'enterTheConversation' y hacerlo hablar.`,       `ACCIDENTE O DESASTRE INESPERADO: Algo sale repentina y c├│micamente mal en el entorno f├¡sico o la acci├│n. Por ejemplo: se rompe un barril y derrama todo, colapsa una repisa, se suelta un animal, cae una gotera tremenda, o un NPC comete un error torpe que revela algo vergonzoso. Narra este incidente y haz que los personajes reaccionen con molestia o asombro.`,       `GOLPE DE SUERTE INESPERADO: Algo sale extraordinariamente bien de forma imprevista. Por ejemplo: descubren una moneda de oro perdida bajo una tabla, un NPC gru├▒├│n tiene un arranque inusual de generosidad y regala algo o confiesa una pista valiosa, o un problema menor se resuelve de forma milagrosa. Refleja esto en el di├ílogo.`,       `DISTURBIO EXTERNO DE IMPREVISTO: Se escucha de repente un grito lejano, un golpe sordo o un estruendo misterioso que proviene de una ubicaci├│n adyacente. El di├ílogo se detiene abruptamente y los personajes presentes reaccionan con p├ínico, sospecha o curiosidad, alentando al jugador a investigar.`     ];     const chosenTwist = twists[Math.floor(Math.random() * twists.length)];     unexpectedEventNote = `\n\n=== EVENTO INESPERADO (15% DE CHANCE ACTIVADO) ===\n` +       `ATENCI├ôN: Para este turno, debes forzar e integrar el siguiente giro inesperado en la narrativa y los di├ílogos:\n${chosenTwist}\n`;     console.log(`[TwistEngine] 15% Chance Twist Triggered: "${chosenTwist.split(':')[0]}"`);   }    // If an event or a twist triggered at this intermediate stop, we halt the travel queue   if ((activeEvent || twistTriggered) && travelQueue.length > 0) {     console.log(`[JourneyEngine] Incident triggered at intermediate stop "${locationId}" (Event: ${activeEvent?.id || 'none'}, Twist: ${twistTriggered}). Halting travel queue.`);     travelQueue = [];     inputState.travelQueue = [];   }    // If this is an intermediate stop and nothing happened, return a fast transition scene   if (travelQueue.length > 0 && !activeEvent && !twistTriggered) {     const stopName = currentLocations.find(l => l.id === locationId)?.name || locationId;     const destName = currentLocations.find(l => l.id === travelQueue[travelQueue.length - 1])?.name || travelQueue[travelQueue.length - 1];      console.log(`[JourneyEngine] Simple transition stop at "${locationId}" during journey to "${destName}". Returning fast transition.`);      const transitionScene = {       locationId: locationId,       participantIds: [],       narration: `En viaje hacia ${destName}.`,       messages: [         {           speakerId: 'narrator',           line: `[Especial] Pasas por ${stopName} en direcci├│n a tu destino final. Todo parece tranquilo aqu├¡ por el momento.`,           expression: 'neutral'         }       ],       relationshipDeltas: [],       trustDeltas: [],       newNpc: null,       newLocation: null,       minutesPassed: travelMinutes,       exitTheConversation: [],       enterTheConversation: [],       updateLocationImage: null,       inventoryDeltas: [],       goldDelta: 0,       questUpdates: [],       state: {         ...inputState,         travelQueue: travelQueue       }     };      // Run day or time phase updates if advanced     const oldTOD = state.timeOfDay || getTimeOfDay(state.time || '08:00');     const newTOD = inputState.timeOfDay;     const isNewDay = inputState.day > (state.day || 1);      if (isNewDay || oldTOD !== newTOD) {       const activeProvider = provider || state.provider || defaultProvider;       let currentClient = client;       let currentModel = modelName;       let currentIsGemini = useGemini;        if (activeProvider === 'gemini' && geminiClient) {         currentClient = geminiClient;         currentModel = geminiModel;         currentIsGemini = true;       } else if (activeProvider === 'openai' && openaiClient) {         currentClient = openaiClient;         currentModel = openaiModel;         currentIsGemini = false;       }        const activeConversationManager = new ConversationManager({         client: currentClient,         model: currentModel,         isGemini: currentIsGemini       });        if (isNewDay) {         console.log(`[QuestEngine] Day advanced to ${inputState.day} during transition. Triggering morning world update...`);         runMorningWorldUpdate(activeConversationManager, inputState.day, history, inputState);       } else {         triggerTimeOfDayTransitionIfNeeded(oldTOD, newTOD, inputState, history, activeConversationManager, db);       }     }      if (transitionScene.narration) {       history.push({         speakerId: 'narrator',         speaker: 'Narrador',         line: transitionScene.narration,         type: 'npc',         locationId: inputState.locationId,         day: inputState.day,         time: inputState.time       });     }     if (Array.isArray(transitionScene.messages)) {       transitionScene.messages.forEach(m => {         history.push({           speakerId: m.speakerId,           speaker: m.speakerId === 'narrator' ? 'Narrador' : m.speakerId,           line: m.line,           type: 'npc',           locationId: inputState.locationId,           day: inputState.day,           time: inputState.time         });       });     }      await saveHistory(history);     transitionScene.history = history;      return res.json(transitionScene);   }    const activeProvider = provider || state.provider || defaultProvider;   let currentClient = client;   let currentModel = modelName;   let currentIsGemini = useGemini;    if (activeProvider === 'gemini' && geminiClient) {     currentClient = geminiClient;     currentModel = geminiModel;     currentIsGemini = true;   } else if (activeProvider === 'openai' && openaiClient) {     currentClient = openaiClient;     currentModel = openaiModel;     currentIsGemini = false;   }    const activeConversationManager = new ConversationManager({     client: currentClient,     model: currentModel,     isGemini: currentIsGemini   });    try {     const scene = await activeConversationManager.createScene({       locationId,       participantIds: finalParticipantIds,       playerText,       history,       state: inputState,       activeEvent,       travelMinutes,       unexpectedEventNote     });      // Check if the DM requested to generate a quest on-the-fly     if (scene.generateQuest && scene.generateQuest.npcId && scene.generateQuest.npcId !== '') {       console.log(`[QuestEngine] Dungeon Master requested on-the-fly quest generation for NPC: "${scene.generateQuest.npcId}" with theme: "${scene.generateQuest.theme}"`);       const newQuest = await generateSingleQuestOnTheFly(         activeConversationManager,          scene.generateQuest.npcId,          scene.generateQuest.urgency || 'media',          scene.generateQuest.theme || 'favor general',          inputState       );       inputState.quests.push(newQuest);       console.log(`[QuestEngine] Successfully registered on-the-fly quest: "${newQuest.title}" (${newQuest.id})`);     }      // Handle dynamic NPC creation     if (scene.newNpc && scene.newNpc.id) {       const currentNpcs = await loadNpcs();       const cleanId = scene.newNpc.id.toLowerCase().replace(/[^a-z0-9]/g, '');       const matchedNpc = currentNpcs.find(npc => npc.id === cleanId || npc.name.toLowerCase().trim() === scene.newNpc.name.toLowerCase().trim());              if (matchedNpc) {         console.log(`[Database] Collision detected for new NPC request: "${scene.newNpc.name}" (ID in request: "${scene.newNpc.id}"). Remapping all references to existing NPC ID "${matchedNpc.id}".`);         // Remap in participantIds         scene.participantIds = scene.participantIds.map(id => id === scene.newNpc.id ? matchedNpc.id : id);         // Remap in messages speakerId         scene.messages = scene.messages.map(m => m.speakerId === scene.newNpc.id ? { ...m, speakerId: matchedNpc.id } : m);         // Nullify newNpc         scene.newNpc = null;       } else {         console.log(`[Database] Registering new NPC generated by Dungeon Master: ${scene.newNpc.name}`);                  const cleanNpc = {           id: cleanId,           name: scene.newNpc.name,           role: scene.newNpc.role,           locationId: scene.newNpc.locationId || locationId,           personality: scene.newNpc.personality,           secret: scene.newNpc.secret,           hint: scene.newNpc.hint,           color: scene.newNpc.color || '#7c7c7c',           skin: scene.newNpc.skin || '#dfab8f',           hair: scene.newNpc.hair || '#2b1b17',           outfit: scene.newNpc.outfit || 'civic',           suggestions: Array.isArray(scene.newNpc.suggestions) ? scene.newNpc.suggestions : ["Hablar", "Preguntar"]         };          const updatedNpcs = [...currentNpcs, cleanNpc];         await saveNpcs(updatedNpcs);          // Generate portraits synchronously so they exist when client receives the response         await generateNpcPortraits(           cleanNpc.id,           cleanNpc.color,           cleanNpc         );          // Sync new NPC to Vector DB         await syncNpcToVectorDb(cleanNpc);       }     }      // Handle dynamic Location creation     if (scene.newLocation && scene.newLocation.id) {       const currentLocations = await loadLocations();       const cleanLocId = scene.newLocation.id.toLowerCase().replace(/[^a-z0-9]/g, '');       const exists = currentLocations.some(loc => loc.id === cleanLocId);        if (!exists) {         console.log(`[Database] Registering new Location generated by Dungeon Master: ${scene.newLocation.name}`);                  // Resolve parent connection node and distance         const parentLocId = String(scene.newLocation.connectedTo || oldLocId).trim().toLowerCase();         const distance = Number(scene.newLocation.distance || 5);                  const cleanLocation = {           id: cleanLocId,           name: scene.newLocation.name,           asset: `assets/locations/${cleanLocId}.png`,           prompt: scene.newLocation.prompt,           ambient: scene.newLocation.ambient || 'Silencio medieval y misterio en el ambiente.',           connections: [             { to: parentLocId, distance: distance }           ]         };          // Mutate parent in current list to establish the bidirectional connection         const parentLoc = currentLocations.find(l => l.id === parentLocId);         if (parentLoc) {           if (!parentLoc.connections) parentLoc.connections = [];           const connExists = parentLoc.connections.some(c => c.to === cleanLocId);           if (!connExists) {             parentLoc.connections.push({ to: cleanLocId, distance: distance });           }         }          const updatedLocations = [...currentLocations, cleanLocation];         await saveLocations(updatedLocations);          // Generate location background synchronously ÔÇö image is ready before response is sent         const generated = await generateLocationBackground(cleanLocation.id, cleanLocation.prompt);          // Attach a cache-busted assetUrl to the scene so the client gets the final URL         const ts = Date.now();         scene.newLocation.id = cleanLocId;         scene.newLocation.assetUrl = generated           ? `assets/locations/${cleanLocId}.png?t=${ts}`           : null;          // Sync new Location to Vector DB         await syncLocationToVectorDb(cleanLocation);       }     }      // Handle AI request to update/overwrite an existing location image     if (scene.updateLocationImage && scene.updateLocationImage.locationId && scene.updateLocationImage.locationId.trim() !== '') {       const locIdToUpdate = String(scene.updateLocationImage.locationId).trim().toLowerCase();       const newPrompt = String(scene.updateLocationImage.prompt || '').trim();              const currentLocations = await loadLocations();       const existingLoc = currentLocations.find((loc) => loc.id === locIdToUpdate);       if (existingLoc && newPrompt) {         console.log(`[Server] Overwriting background image for location: ${locIdToUpdate} with prompt: "${newPrompt}"`);         existingLoc.prompt = newPrompt;         await saveLocations(currentLocations);          // Generate the new background image synchronously to overwrite the file         await generateLocationBackground(locIdToUpdate, newPrompt);                  // Attach updated location info to the payload response so the client knows it changed         const ts = Date.now();         scene.locationUpdate = {           id: locIdToUpdate,           prompt: newPrompt,           assetUrl: `assets/locations/${locIdToUpdate}.png?t=${ts}`         };          // Re-sync to Vector DB         await syncLocationToVectorDb(existingLoc);       }     }      // Resolve narrative travel if the AI shifted the location ID     const postLocId = scene.locationId || locationId;     let aiTravelMinutes = 0;     if (locationId !== postLocId) {       const path = findPath(locationId, postLocId);       if (path && path.length > 2) {         // AI attempted a multi-node jump. We step to the next adjacent node and queue the rest!         const nextStepLocId = path[1];         const remainingQueue = path.slice(2);                  const conn = (CONNECTIONS[locationId] || []).find(c => c.to === nextStepLocId);         aiTravelMinutes = conn ? conn.distance : 5;                  scene.locationId = nextStepLocId;         inputState.travelQueue = remainingQueue;                  console.log(`[JourneyEngine] Intercepted AI jump from "${locationId}" to "${postLocId}". Forcing step to "${nextStepLocId}". Remaining queue: [${remainingQueue.join(', ')}].`);                  const destName = currentLocations.find(l => l.id === postLocId)?.name || postLocId;         const nextStepName = currentLocations.find(l => l.id === nextStepLocId)?.name || nextStepLocId;                  // Discard the AI's generated messages for the final destination to avoid spatial contradictions.         // Replace it with a clean narrator transition for the intermediate step.         scene.messages = [           {             speakerId: 'narrator',             line: `[Especial] Te trasladas hacia ${nextStepName} (parada intermedia en tu viaje hacia ${destName}).`,             expression: 'neutral'           }         ];         scene.participantIds = []; // clear participants for the transition stop         scene.narration = `En viaje hacia ${destName}.`;       } else if (path && path.length === 2) {         // Direct adjacent travel         aiTravelMinutes = getPathDistance(path);         console.log(`[TravelEngine] AI narratively moved player from "${locationId}" to "${postLocId}" (${aiTravelMinutes} min).`);                  const destName = currentLocations.find(l => l.id === postLocId)?.name || postLocId;         scene.messages.push({           speakerId: 'narrator',           line: `[Especial] Te trasladas a ${destName} (${aiTravelMinutes} min de viaje).`,           expression: 'neutral'         });       }     }      // Ingestion of older dialogue turns is now handled during createScene before generation,     // which prevents indexing the active sliding window turns and avoids prompt clutter.      console.log(`[Server] Success scene generation - locationId: "${scene.locationId}", participantIds: [${scene.participantIds.join(', ')}], messages: ${scene.messages.length}`);     scene.messages.forEach((m, idx) => {       console.log(`  [Msg ${idx + 1}] (${m.speakerId}): "${m.line}"`);     });      const minutes = (scene.minutesPassed || 2) + aiTravelMinutes;     const { day: nextDay, time: nextTime } = addMinutesToTime(inputState.day, inputState.time, minutes);          const oldTOD = state.timeOfDay || getTimeOfDay(state.time || '08:00');     const isNewDay = nextDay > (state.day || 1);          inputState.day = nextDay;     inputState.time = nextTime;     inputState.timeOfDay = getTimeOfDay(nextTime);     const newTOD = inputState.timeOfDay;      if (isNewDay) {       console.log(`[QuestEngine] Day advanced to ${nextDay}. Triggering morning world update in the background...`);       runMorningWorldUpdate(activeConversationManager, nextDay, history, inputState);     } else {       triggerTimeOfDayTransitionIfNeeded(oldTOD, newTOD, inputState, history, activeConversationManager, db);     }      const bgEndResult = await triggerBackgroundEvents(inputState, events, currentLocations);      const allBgNotifications = [...(bgStartResult?.notifications || []), ...(bgEndResult?.notifications || [])];     if (allBgNotifications.length > 0) {       scene.messages.unshift(...allBgNotifications);     }      // Accumulate relationships and trust deltas (add instead of replace)     const updatedRelationships = { ...inputState.relationships };     if (scene.relationshipDeltas) {       Object.entries(scene.relationshipDeltas).forEach(([npcId, delta]) => {         updatedRelationships[npcId] = (updatedRelationships[npcId] || 0) + delta;       });     }      const updatedTrust = { ...inputState.trust };     if (scene.trustDeltas) {       Object.entries(scene.trustDeltas).forEach(([npcId, delta]) => {         updatedTrust[npcId] = (updatedTrust[npcId] || 0) + delta;       });     }      // Apply inventory deltas     if (Array.isArray(scene.inventoryDeltas)) {       scene.inventoryDeltas.forEach(delta => {         if (delta.action === 'add') {           inputState.inventory.push({             id: delta.id,             name: delta.name,             description: delta.description || ''           });         } else if (delta.action === 'remove') {           inputState.inventory = inputState.inventory.filter(item => item.id !== delta.id);         }       });     }      // Apply gold delta     if (typeof scene.goldDelta === 'number') {       inputState.gold = (inputState.gold || 0) + scene.goldDelta;     }      // Apply quest updates     const updatedQuests = inputState.quests.map(q => {       let triggerDirectMeet = q.triggerDirectMeet;       if (q.status === 'active' && q.urgency === 'alta' && q.triggerDirectMeet === true) {         triggerDirectMeet = false;       }       const update = Array.isArray(scene.questUpdates) ? scene.questUpdates.find(u => u.id === q.id) : null;       const status = update ? update.status : q.status;       return { ...q, status, triggerDirectMeet };     });      scene.state = {       relationships: updatedRelationships,       trust: updatedTrust,       day: inputState.day,       time: inputState.time,       timeOfDay: inputState.timeOfDay,       flags: inputState.flags,       completedEvents: inputState.completedEvents,       travelQueue: inputState.travelQueue,       inventory: inputState.inventory,       gold: inputState.gold,       quests: updatedQuests,       npcActivityLog: inputState.npcActivityLog || []     };      if (scene.narration) {       history.push({         speakerId: 'narrator',         speaker: 'Narrador',         line: scene.narration,         type: 'npc',         locationId: inputState.locationId,         day: inputState.day,         time: inputState.time       });     }     if (Array.isArray(scene.messages)) {       scene.messages.forEach(m => {         history.push({           speakerId: m.speakerId,           speaker: currentNpcs.find(n => n.id === m.speakerId)?.name || m.speakerId,           line: m.line,           type: 'npc',           locationId: inputState.locationId,           day: inputState.day,           time: inputState.time         });       });     }      await saveHistory(history);     scene.history = history;      return res.json(scene);   } catch (error) {     console.error('[Server] Conversation manager error:', error);     const fallback = activeConversationManager.makeFallbackScene({ locationId, participantIds: finalParticipantIds, playerText, state: inputState });          const { day: nextDay, time: nextTime } = addMinutesToTime(inputState.day, inputState.time, 2);     inputState.day = nextDay;     inputState.time = nextTime;      fallback.generateQuest = null;      const fallbackRelationships = { ...inputState.relationships };     if (fallback.relationshipDeltas) {       Object.entries(fallback.relationshipDeltas).forEach(([npcId, delta]) => {         fallbackRelationships[npcId] = (fallbackRelationships[npcId] || 0) + delta;       });     }      const fallbackTrust = { ...inputState.trust };     if (fallback.trustDeltas) {       Object.entries(fallback.trustDeltas).forEach(([npcId, delta]) => {         fallbackTrust[npcId] = (fallbackTrust[npcId] || 0) + delta;       });     }      fallback.state = {       relationships: fallbackRelationships,       trust: fallbackTrust,       day: inputState.day,       time: inputState.time,       timeOfDay: getTimeOfDay(inputState.time),       flags: inputState.flags,       completedEvents: inputState.completedEvents,       travelQueue: inputState.travelQueue || [],       inventory: inputState.inventory || [],       gold: inputState.gold || 0,       quests: inputState.quests || [],       npcActivityLog: inputState.npcActivityLog || []     };      console.log(`[Server] Responding with Fallback Scene - messages: ${fallback.messages.length}`);     fallback.messages.forEach((m, idx) => {       console.log(`  [Fallback Msg ${idx + 1}] (${m.speakerId}): "${m.line}"`);     });      if (fallback.narration) {       history.push({         speakerId: 'narrator',         speaker: 'Narrador',         line: fallback.narration,         type: 'npc',         locationId: inputState.locationId,         day: inputState.day,         time: inputState.time       });     }     if (Array.isArray(fallback.messages)) {       fallback.messages.forEach(m => {         history.push({           speakerId: m.speakerId,           speaker: currentNpcs.find(n => n.id === m.speakerId)?.name || m.speakerId,           line: m.line,           type: 'npc',           locationId: inputState.locationId,           day: inputState.day,           time: inputState.time         });       });     }      await saveHistory(history);     fallback.history = history;      return res.json(fallback);   } }); }